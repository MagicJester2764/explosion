# Host-side tools for a cross build: wayland-scanner and expat, built here
# because a cross build still needs them to run on this machine.
[built-in options]
pkg_config_path = '@HOSTPREFIX@/lib64/pkgconfig:@HOSTPREFIX@/lib/pkgconfig'
